import { RegisterView } from 'src/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Social Z - Register',
};

export default function LoginPage() {
  return <RegisterView />;
}
